services:
  docs:
    image: secretshunter_local_docs
    container_name: secretshunter_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./secretshunter:/app/secretshunter:z
    ports:
      - '9000:9000'
    command: /start-docs
